# プロジェクト概要

## 背景

近年、多くのプロダクトで observability（観測性）の重要性が高まっており、tracing / logging / metrics などを通じてシステムの状態を可視化することが一般的になっています。
一方で、これらのテレメトリデータに個人情報（PII: Personally Identifiable Information）が含まれることによるセキュリティリスクが問題となっています。

特に、Datadog や OpenTelemetry などの外部サービスにテレメトリを送信する場合、

* 個人情報の漏洩
* コンプライアンス違反
* 監査対応コストの増加

といったリスクが顕在化します。

## 課題

Rust の `tracing` クレートでは `#[instrument]` マクロにより関数引数を自動的に span に記録できますが、デフォルトでは以下のような挙動になります。

* すべての引数が自動的に記録される（opt-out）
* `skip` / `skip_all` を明示的に書く必要がある

このため、以下の問題が発生します。

* 書き忘れによる PII の漏洩
* チーム全体での運用ルール依存
* 静的に安全性を保証できない

## 目標

本プロジェクトでは以下を目標とします。

1. テレメトリに出力される値を型レベルで制御する
2. PII を誤って出力することをコンパイル時に防ぐ
3. tracing に依存しない汎用的な設計にする
4. ergonomics（使いやすさ）を損なわない

## アプローチ

### 従来アプローチ

* `skip_all` を必ず付与する
* `fields(...)` で opt-in

問題点:

* 人間の注意力に依存
* コンパイル時に保証できない

### 提案アプローチ（telemetry-safe）

* 値オブジェクトに `ToTelemetry` trait を実装
* テレメトリ出力はこの trait を経由させる
* trait 未実装の型はコンパイルエラー

これにより:

* 「出してよい値」だけが明示される
* PII の漏洩を構造的に防ぐ

## ライブラリのスコープ

本ライブラリは tracing に依存しない。

対象:

* logging
* tracing
* metrics
* error reporting

すべてに共通する「安全な値の表現」を扱う。

## 提供価値

* セキュリティ: PII の漏洩防止
* 型安全: compile-time enforcement
* DDD 親和性: 値オブジェクト主導の設計
* 再利用性: 複数の observability backend に対応
